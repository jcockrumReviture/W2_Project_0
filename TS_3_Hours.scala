class TS_3_InHours {

    // Enter Date (Default Now)
    // If {Date == Has Values} Show existing Punches Else Only Show Start
        // 1 Date 
    // If {Start == NULL} Calculate Other Values {out-Lunch,In-lunch,End} on 8 hour work day    
        // 2 Start 
    // After Start <> Null Remaining options show
        // 3 Out - Lunch {Punch Info}
        // 4  In - Lunch {Punch Info}
        // 5 Out {Punch Info}
}
